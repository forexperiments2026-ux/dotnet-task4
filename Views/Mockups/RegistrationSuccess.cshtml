@{
    ViewData["Title"] = "Registration success";
    var registrationStatus = TempData["RegistrationStatus"] as string;
    var registeredEmail = TempData["RegisteredEmail"] as string;
}

<section class="status-page mx-auto" style="max-width: 720px;">
    <div class="alert alert-success shadow-sm" role="status">
        @(string.IsNullOrWhiteSpace(registrationStatus)
            ? "Registration completed. Confirmation e-mail is queued and will be sent asynchronously."
            : registrationStatus)
    </div>

    <div class="card shadow-sm">
        <div class="card-body p-4">
            <h1 class="h4">Что видит пользователь после регистрации</h1>
            <ul class="mb-0">
                <li>Аккаунт создан сразу со статусом <strong>unverified</strong>.</li>
                @if (!string.IsNullOrWhiteSpace(registeredEmail))
                {
                    <li>Зарегистрированный e-mail: <strong>@registeredEmail</strong>.</li>
                }
                <li>Пользователь уже может войти в систему.</li>
                <li>Переход по ссылке из e-mail меняет статус на <strong>active</strong>.</li>
            </ul>
        </div>
    </div>

    <div class="d-flex flex-wrap gap-2 mt-3">
        <a class="btn btn-primary" asp-controller="Mockups" asp-action="Login">Go to login</a>
        <a class="btn btn-outline-secondary" asp-controller="Mockups" asp-action="Index">Back to mockups list</a>
    </div>
</section>
